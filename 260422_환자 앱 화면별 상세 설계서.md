# 환자 앱 화면별 상세 설계서

## 1. 개요

**문서 목적:** PRD(수면장애 환자 관리 시스템)의 환자용 화면 요구사항을 **구현 가능한 수준의 화면 단위 명세**로 변환한 상세 설계서

**대상 플랫폼:** 모바일 우선 웹앱 (PWA)

**기술 스택:** Next.js + Tailwind CSS + shadcn/ui + Recharts + Supabase

### 하단 탭 구조 (5개)

| **아이콘** | **탭명** | **핵심 역할** |
| --- | --- | --- |
| 🏠 | 홈 | 오늘의 수면 일지 작성, 복약 체크, 할 일 현황 |
| 📊 | 기록 | 수면 패턴 그래프, 수면 효율, 검사 결과(HRV·QEEG·InBody), ISI 점수 |
| 💊 | 처방 | 현재 처방 정보 조회, 복약 지시사항 |
| 📋 | 자가진단 | ISI 7문항 입력·제출, 점수 이력 그래프 |
| 💬 | 문의 | Q&A 작성, 답변 확인 |

### 공통 UI 원칙

- 텍스트 입력 최소화, **슬라이더·버튼 선택** 방식 우선
- 색상 톤: 화이트 + 소프트 그레이 배경, 소프트 블루 액센트
- 하단 탭 내비게이션 고정, 큰 터치 영역(최소 44px)
- PWA 지원: 홈 화면 추가, 오프라인 기본 안내

---

## 2. 🏠 홈 탭

### 2.1 화면 구성 요소

```jsx
┌─────────────────────────────┐
│  👋 {환자명}님, 좋은 아침이에요   │
├─────────────────────────────┤
│  📝 오늘의 할 일               │
│  ┌───────────────────────┐  │
│  │ ○ 수면 일지 작성하기      │  │
│  │ 💊 한약                  │  │
│  │ ○ 아침 ○ 점심 ○ 저녁 ○ 취침전│  │
│  │ 💊 양약                  │  │
│  │ ○ 아침 ○ 점심 ○ 저녁 ○ 취침전│  │
│  └───────────────────────┘  │
│  * 처방 환자에게만 노출         │
├─────────────────────────────┤
│  📊 어제 수면 요약              │
│  ┌───────────────────────┐  │
│  │ 수면 시간: 6h 30m        │  │
│  │ 수면 효율: 82% ⚠️        │  │
│  │ 만족도: ★★★☆☆           │  │
│  └───────────────────────┘  │
├─────────────────────────────┤
│  ⚙️ 알림 설정                  │
│  수면일지 리마인드  [OFF]       │
│  복약 알림 *       [OFF]       │
│  Q&A 답변 알림     [OFF]       │
└─────────────────────────────┘
```

### 2.2 데이터 흐름

| **UI 요소** | **Supabase 테이블** | **읽기 컬럼** | **쓰기 컬럼** |
| --- | --- | --- | --- |
| 환자명 인사 | `patients` | `name` | — |
| 수면 일지 작성 여부 | `sleep_diary` | `diary_date` (오늘 레코드 존재 여부) | — |
| 복약 체크 버튼 | `sleep_diary` | `herbal_morning`, `herbal_lunch`, `herbal_evening`, `herbal_bedtime`, `western_morning`, `western_lunch`, `western_evening`, `western_bedtime` | (동일 컬럼) |
| 복약 메뉴 노출 조건 | `treatment_records` | 활성 처방 존재 여부 | — |
| 어제 수면 요약 | `sleep_diary` | `bedtime`, `wake_time`, `sleep_quality`, `sleep_onset_latency`, `night_awakening_count` | — |
| 알림 설정 토글 | `settings` | `push_enabled` | `push_enabled` |

### 2.3 API 엔드포인트

| **Method** | **Path** | **설명** |
| --- | --- | --- |
| `GET` | `/api/home/summary` | 오늘 할 일 상태 + 어제 수면 요약 통합 조회. 처방 존재 여부 포함 |
| `PATCH` | `/api/medication/check` | 복약 체크 토글 `{ med_type: "herbal" | "western", timing: "morning" | "lunch" | "evening" | "bedtime", checked: boolean }` |
| `GET` | `/api/settings` | 현재 알림 설정 조회 |
| `PATCH` | `/api/settings` | 알림 설정 업데이트 `{ diary_remind: bool, med_alarm: bool, qna_alarm: bool }` |

### 2.4 UI 상태 처리

- **로딩:** 스켈레톤 UI (할 일 카드 3줄 + 수면 요약 카드)
- **수면 일지 미작성:** "수면 일지 작성하기" 버튼 강조(Primary 색상)
- **수면 일지 작성 완료:** 체크 표시 + 비활성화
- **처방 없는 환자:** 복약 체크 항목 자체가 렌더링되지 않음
- **에러:** 토스트 메시지 "데이터를 불러올 수 없습니다. 다시 시도해 주세요."

---

## 3. 📊 기록 탭

### 3.1 화면 구성 요소

```
┌─────────────────────────────┐
│  📊 내 기록                    │
│  [수면] [효율] [검사] [ISI]     │  ← 서브탭
├─────────────────────────────┤
│                             │
│  (선택된 서브탭에 따라 변경)     │
│                             │
└─────────────────────────────┘
```

**서브탭 4개:**

### 3.1.1 수면 서브탭

- **수면 패턴 캘린더:** 월별 캘린더에 각 날짜의 수면 만족도 색상 표시 (1=빨강 ~ 5=파랑)
- **주간 수면 시간 바 차트:** 최근 7일간 취침~기상 시간 시각화
- **수면 단계 스택 바 차트:** 관리자 입력 데이터 기반 (총 수면/깊은 수면/얕은 수면/REM)
- **복약 이력 타임라인:** 처방 환자 한정, 한약·양약별 아침·점심·저녁·취침전 복약 체크 이력
- **증상 변화 타임라인:** 날짜별 컨디션 점수 추이

### 3.1.2 효율 서브탭

- **수면 효율(%) 꺾은선 그래프:** 날짜별 추이
- **기준선 표시:** 85%(정상 경계), 70%(불량 경계) 수평선
- **색상 코딩:** 85% 이상 🟢 / 85% 미만 🟡 / 70% 미만 🔴
- **계산식:** `수면 효율 = 실제 수면 시간 ÷ (기상 시간 - 취침 시간) × 100`

### 3.1.3 검사 서브탭

검사 유형별 **3개 하위 탭**으로 분리:

**① HRV 검사 결과**

- 날짜별 HRV 검사 결과 카드 리스트
- 각 카드: 검사일, 주요 수치(`result_data` JSONB에서 추출), 원장 코멘트(`summary`)
- 최근 검사 결과를 상단에 하이라이트

**② QEEG 검사 결과**

- 날짜별 QEEG 검사 결과 카드 리스트
- 각 카드: 검사일, 뇌파 관련 주요 수치, 원장 코멘트
- QEEG 특성상 수치 항목이 많을 수 있으므로 접기/펼치기(Accordion) UI

**③ InBody 검사 결과**

- 날짜별 InBody 검사 결과 카드 리스트
- 각 카드: 검사일, 체성분 주요 수치(체중, 골격근량, 체지방률 등), 원장 코멘트
- 주요 지표 추이 꺾은선 그래프 (복수 검사 시)

### 3.1.4 ISI 서브탭

- **ISI 점수 꺾은선 그래프:** 날짜별 총점(0~28) 추이
- **기준선 표시:** 7(없음/경미 경계), 14(경미/중등도 경계), 21(중등도/심각 경계)
- **최근 진단 결과 배지:** 없음 🟢 / 경미 🟡 / 중등도 🟠 / 심각 🔴

### 3.2 데이터 흐름

| **서브탭** | **Supabase 테이블** | **주요 컬럼** |
| --- | --- | --- |
| 수면 | `sleep_diary` | `diary_date`, `bedtime`, `wake_time`, `sleep_quality`, `morning_fatigue`, `condition`, `total_sleep_min`, `deep_sleep_min`, `light_sleep_min`, `rem_sleep_min`, `herbal_morning`, `herbal_lunch`, `herbal_evening`, `herbal_bedtime`, `western_morning`, `western_lunch`, `western_evening`, `western_bedtime` |
| 효율 | `sleep_diary` | `bedtime`, `wake_time`, `sleep_onset_latency`, `night_awakening_count` → 프론트에서 효율 계산 |
| 검사 — HRV | `exam_results` | `exam_date`, `result_data`, `summary` WHERE `exam_type = 'HRV'` |
| 검사 — QEEG | `exam_results` | `exam_date`, `result_data`, `summary` WHERE `exam_type = 'QEEG'` |
| 검사 — InBody | `exam_results` | `exam_date`, `result_data`, `summary` WHERE `exam_type = 'InBody'` |
| ISI | `isi_assessments` | `assessed_at`, `q1`~`q7`, `total_score` |

### 3.3 API 엔드포인트

| **Method** | **Path** | **설명** |
| --- | --- | --- |
| `GET` | `/api/records/sleep?range=7d|30d|90d` | 수면 일지 기록 목록 (기간 필터). 캘린더·차트용 집계 데이터 포함 |
| `GET` | `/api/records/efficiency?range=30d|90d` | 날짜별 수면 효율 배열 반환 |
| `GET` | `/api/records/exams/hrv` | HRV 검사 결과 목록 (날짜 역순) |
| `GET` | `/api/records/exams/qeeg` | QEEG 검사 결과 목록 (날짜 역순) |
| `GET` | `/api/records/exams/inbody` | InBody 검사 결과 목록 (날짜 역순) |
| `GET` | `/api/records/isi?range=90d|all` | ISI 자가진단 점수 이력 |

### 3.4 UI 상태 처리

- **데이터 없음(수면):** "아직 기록이 없어요. 홈에서 수면 일지를 작성해 보세요." + 홈 탭 이동 버튼
- **데이터 없음(검사):** "아직 등록된 {HRV/QEEG/InBody} 검사 결과가 없어요."
- **관리자 수면 데이터 없음:** 수면 단계 스택 바 차트 영역 숨김 (환자 입력 데이터만 표시)
- **로딩:** 차트 영역 스켈레톤 + 스피너

---

## 4. 💊 처방 탭

### 4.1 화면 구성 요소

```
┌─────────────────────────────┐
│  💊 내 처방                    │
├─────────────────────────────┤
│  현재 처방                     │
│  ┌───────────────────────┐  │
│  │ 📋 가감귀비탕            │  │
│  │ 처방 기간: 4/1 ~ 4/28   │  │
│  │ ──────────────────     │  │
│  │ 복약 지시사항:           │  │
│  │ • 아침 식후 30분 1포     │  │
│  │ • 저녁 식후 30분 1포     │  │
│  │ ──────────────────     │  │
│  │ 진료 메모:              │  │
│  │ 수면 유도 목적, 2주 후   │  │
│  │ 경과 확인 예정           │  │
│  └───────────────────────┘  │
├─────────────────────────────┤
│  지난 처방 이력               │
│  ▸ 청심온담탕 (3/1 ~ 3/28)    │
│  ▸ 가미온담탕 (2/1 ~ 2/28)    │
└─────────────────────────────┘
```

### 4.2 데이터 흐름

| **UI 요소** | **Supabase 테이블** | **주요 컬럼** |
| --- | --- | --- |
| 현재 처방 카드 | `treatment_records` | `prescription`, `treatment_notes`, `visit_date` — 가장 최근 레코드 |
| 지난 처방 이력 | `treatment_records` | 전체 레코드 (날짜 역순) |

### 4.3 API 엔드포인트

| **Method** | **Path** | **설명** |
| --- | --- | --- |
| `GET` | `/api/prescriptions/current` | 현재 활성 처방 조회 (처방 기간 내 최신 레코드) |
| `GET` | `/api/prescriptions/history` | 전체 처방 이력 (페이지네이션 지원, 20건 단위) |

### 4.4 UI 상태 처리

- **처방 없음:** "현재 처방된 한약이 없습니다." 안내 메시지
- **처방 만료:** "처방 기간이 종료되었습니다." + 과거 이력으로 이동

---

## 5. 📋 자가진단 탭 (ISI)

### 5.1 화면 구성 요소

```
┌─────────────────────────────┐
│  📋 불면증 자가진단 (ISI)       │
├─────────────────────────────┤
│  7문항 입력 폼                 │
│  Q1. 잠들기 어려움     [0~4]   │
│  Q2. 수면 유지 어려움   [0~4]   │
│  Q3. 너무 일찍 깸      [0~4]   │
│  Q4. 수면 패턴 만족도   [0~4]   │
│  Q5. 낮 기능 저하      [0~4]   │
│  Q6. 수면 문제 인지    [0~4]   │
│  Q7. 수면 문제 걱정    [0~4]   │
│                             │
│  [      제출하기      ]       │
├─────────────────────────────┤
│  📊 제출 후 결과 표시          │
│  ┌───────────────────────┐  │
│  │ 총점: 18점 / 28점       │  │
│  │ 🟠 중등도 불면증         │  │
│  └───────────────────────┘  │
├─────────────────────────────┤
│  📈 점수 이력 그래프          │
│  (날짜별 꺾은선 그래프)        │
│  기준선: 7 / 14 / 21         │
└─────────────────────────────┘
```

### 5.2 ISI 점수 해석 기준

| **점수 범위** | **단계** | **색상** | **설명** |
| --- | --- | --- | --- |
| 0 ~ 7 | 없음 | 🟢 Green | 임상적으로 유의미한 불면증 없음 |
| 8 ~ 14 | 경미 | 🟡 Yellow | 역치 이하 불면증 |
| 15 ~ 21 | 중등도 | 🟠 Orange | 중등도 불면증 |
| 22 ~ 28 | 심각 | 🔴 Red | 심각한 불면증 |

### 5.3 데이터 흐름

| **UI 요소** | **Supabase 테이블** | **주요 컬럼** |
| --- | --- | --- |
| 7문항 입력 폼 | `isi_assessments` | `q1`~`q7` (각 0~4 정수), `assessed_at` |
| 총점 및 단계 | `isi_assessments` | `total_score` (DB Generated Column) |
| 점수 이력 그래프 | `isi_assessments` | `assessed_at`, `total_score` (전체 이력) |

### 5.4 API 엔드포인트

| **Method** | **Path** | **설명** |
| --- | --- | --- |
| `POST` | `/api/isi/submit` | ISI 7문항 제출 `{ q1~q7: number }` → 총점·단계 반환 |
| `GET` | `/api/isi/history` | 전체 ISI 이력 (날짜 역순) |
| `GET` | `/api/isi/latest` | 가장 최근 ISI 결과 조회 |

### 5.5 UI 상태 처리

- **입력 중:** 각 문항은 0~4 버튼 5개 가로 배열, 선택 시 즉시 하이라이트
- **미완성 제출 시도:** 미응답 문항 강조 + "모든 문항에 답해 주세요" 안내
- **제출 완료:** 결과 카드 애니메이션 표시 + 이력 그래프 자동 갱신
- **이력 없음:** "아직 자가진단 기록이 없어요. 위에서 바로 시작할 수 있어요."

---

## 6. 💬 문의 탭 (Q&A)

### 6.1 화면 구성 요소

```
┌─────────────────────────────┐
│  💬 문의하기                   │
│  [ + 새 문의 작성 ]           │
├─────────────────────────────┤
│  내 문의 목록                  │
│  ┌───────────────────────┐  │
│  │ 🟢 약 복용 시간 문의      │  │
│  │    4/18 · 답변 완료       │  │
│  ├───────────────────────┤  │
│  │ 🟡 수면 중 경련 증상      │  │
│  │    4/20 · 답변 대기       │  │
│  └───────────────────────┘  │
├─────────────────────────────┤
│  문의 상세 (탭 또는 모달)      │
│  ┌───────────────────────┐  │
│  │ Q: 약 복용 시간 문의      │  │
│  │ 식전에 먹어도 되나요?     │  │
│  │ ──────────────────     │  │
│  │ A: 식후 30분에 복용해     │  │
│  │    주세요.               │  │
│  └───────────────────────┘  │
└─────────────────────────────┘
```

### 6.2 데이터 흐름

| **UI 요소** | **Supabase 테이블** | **주요 컬럼** |
| --- | --- | --- |
| 문의 목록 | `qna` | `id`, `question`, `is_answered`, `created_at` |
| 문의 상세 | `qna` | `question`, `answer`, `answered_at` |
| 새 문의 작성 | `qna` | `question`, `patient_id`, `created_at` |

### 6.3 API 엔드포인트

| **Method** | **Path** | **설명** |
| --- | --- | --- |
| `GET` | `/api/qna?status=all|pending|answered` | 본인 문의 목록 조회 (상태 필터, 페이지네이션) |
| `GET` | `/api/qna/:id` | 문의 상세 조회 (질문 + 답변) |
| `POST` | `/api/qna` | 새 문의 작성 `{ question: string }` |

### 6.4 UI 상태 처리

- **문의 없음:** "아직 문의 내역이 없어요. 궁금한 점이 있으면 편하게 문의해 주세요."
- **답변 대기:** 🟡 노란 배지 + "답변 대기" 라벨
- **답변 완료:** 🟢 초록 배지 + "답변 완료" 라벨
- **작성 폼:** 텍스트 입력 (최대 1000자) + 제출 버튼
- **제출 성공:** 토스트 "문의가 등록되었습니다." + 목록 자동 갱신

---

## 7. 🏠 홈 → 수면 일지 작성 (전체 화면 플로우)

> 홈 탭에서 "수면 일지 작성하기"를 탭하면 진입하는 전체 화면 폼
> 

### 7.1 입력 폼 구성 (스텝 방식)

**Step 1 — 기본 수면 정보**

- 취침 시간: 시:분 피커 또는 직접 입력 (시·분 각각 숫자 키패드 입력 가능)
- 기상 시간: 시:분 피커 또는 직접 입력 (시·분 각각 숫자 키패드 입력 가능)
- 잠들기까지 걸린 시간: 4개 버튼 (0~10분 / 10~30분 / 30~60분 / 60분 이상)
- 야간 각성 횟수: 4개 버튼 (없음 / 1회 / 2회 / 3회 이상)
- 총 수면 시간: 자동 계산 표시 (참고용)
- 수면 중 특이사항: 텍스트 입력 (선택, 200자)

**Step 2 — 수면의 질 평가**

- 수면 만족도: 1~5 슬라이더
- 기상 시 피로도: 1~5 슬라이더
- 낮 동안 졸림: 3개 버튼 (없음 / 약간 / 심함)

**Step 3 — 추가 상태 기록**

- 낮잠 여부: 토글 (있으면 시간대·시간 입력)
- 꿈: 3개 버튼 (없음 / 기억 안 남 / 꿈을 꿈)
- 카페인 섭취: 4개 버튼 (없음 / 오전 / 오후 / 저녁)
- 음주: 2개 버튼 (없음 / 있음)
- 오늘 컨디션: 1~5 슬라이더
- 특이사항 메모: 텍스트 입력 (선택, 200자)

**Step 4 — 복약 체크 (처방 환자만)**

한약과 양약을 구분하여 각각 4개 시간대별 체크:

**한약 💊**

- 아침: 체크 버튼
- 점심: 체크 버튼
- 저녁: 체크 버튼
- 취침전: 체크 버튼

**양약 💊**

- 아침: 체크 버튼
- 점심: 체크 버튼
- 저녁: 체크 버튼
- 취침전: 체크 버튼

> 한약/양약 처방이 없는 항목은 해당 섹션 자체가 비노출
> 

**제출 → 결과 요약 카드 표시 → 홈으로 복귀**

### 7.2 API 엔드포인트

| **Method** | **Path** | **설명** |
| --- | --- | --- |
| `POST` | `/api/diary` | 수면 일지 전체 제출. Step 1~4 데이터 통합 전송 |
| `GET` | `/api/diary/today` | 오늘 일지 존재 여부 + 기존 데이터 조회 (수정 시) |
| `PUT` | `/api/diary/:id` | 기존 일지 수정 |

### 7.3 Request Body 스키마

```json
{
  "diary_date": "2026-04-22",
  "bedtime": "23:30",
  "wake_time": "06:00",
  "sleep_onset_latency": "10~30분",
  "night_awakening_count": "1회",
  "sleep_event_memo": "",
  "sleep_quality": 3,
  "morning_fatigue": 2,
  "daytime_sleepiness": "약간",
  "nap_taken": false,
  "nap_duration_min": null,
  "dream": "기억 안 남",
  "caffeine": "오전",
  "alcohol": false,
  "condition": 3,
  "memo": "",
  "herbal_morning": true,
  "herbal_lunch": true,
  "herbal_evening": false,
  "herbal_bedtime": false,
  "western_morning": true,
  "western_lunch": false,
  "western_evening": true,
  "western_bedtime": false
}
```

---

## 8. 인증 플로우

### 8.1 환자 로그인

```
┌─────────────────────────────┐
│  🏥 수면장애 클리닉             │
│     환자 로그인                │
│                             │
│  등록번호: [____________]     │
│  비밀번호: [____________]     │
│                             │
│  [       로그인       ]       │
└─────────────────────────────┘
```

### 8.2 인증 API

| **Method** | **Path** | **설명** |
| --- | --- | --- |
| `POST` | `/api/auth/login` | 환자 로그인 `{ registration_number, password }` → JWT 반환 |
| `POST` | `/api/auth/logout` | 로그아웃 (서버 측 토큰 무효화) |
| `GET` | `/api/auth/me` | 현재 로그인 환자 정보 조회 |

### 8.3 보안 규칙

- JWT 액세스 토큰: **1시간** 만료
- 리프레시 토큰: **7일** 만료
- 비활동 **30분** 후 자동 로그아웃
- 로그인 실패 **5회** 시 10분 잠금
- 모든 API: `Authorization: Bearer {token}` 필수
- RLS: `patient_id = auth.uid()` 조건으로 본인 데이터만 접근

---

## 9. 전체 API 엔드포인트 요약

| **Method** | **Path** | **탭** | **인증** |
| --- | --- | --- | --- |
| `POST` | `/api/auth/login` | 로그인 | ❌ |
| `POST` | `/api/auth/logout` | 로그인 | ✅ |
| `GET` | `/api/auth/me` | 공통 | ✅ |
| `GET` | `/api/home/summary` | 🏠 홈 | ✅ |
| `PATCH` | `/api/medication/check` | 🏠 홈 | ✅ |
| `GET` | `/api/settings` | 🏠 홈 | ✅ |
| `PATCH` | `/api/settings` | 🏠 홈 | ✅ |
| `POST` | `/api/diary` | 🏠 홈 | ✅ |
| `GET` | `/api/diary/today` | 🏠 홈 | ✅ |
| `PUT` | `/api/diary/:id` | 🏠 홈 | ✅ |
| `GET` | `/api/records/sleep` | 📊 기록 | ✅ |
| `GET` | `/api/records/efficiency` | 📊 기록 | ✅ |
| `GET` | `/api/records/exams/hrv` | 📊 기록 | ✅ |
| `GET` | `/api/records/exams/qeeg` | 📊 기록 | ✅ |
| `GET` | `/api/records/exams/inbody` | 📊 기록 | ✅ |
| `GET` | `/api/records/isi` | 📊 기록 | ✅ |
| `GET` | `/api/prescriptions/current` | 💊 처방 | ✅ |
| `GET` | `/api/prescriptions/history` | 💊 처방 | ✅ |
| `POST` | `/api/isi/submit` | 📋 자가진단 | ✅ |
| `GET` | `/api/isi/history` | 📋 자가진단 | ✅ |
| `GET` | `/api/isi/latest` | 📋 자가진단 | ✅ |
| `GET` | `/api/qna` | 💬 문의 | ✅ |
| `GET` | `/api/qna/:id` | 💬 문의 | ✅ |
| `POST` | `/api/qna` | 💬 문의 | ✅ |

---

## 10. Next.js 파일 구조 (권장)

```
src/
├── app/
│   ├── layout.tsx              # 루트 레이아웃
│   ├── login/
│   │   └── page.tsx            # 로그인 페이지
│   ├── (patient)/              # 환자 인증 필요 그룹
│   │   ├── layout.tsx          # 하단 탭 내비게이션 포함
│   │   ├── home/
│   │   │   └── page.tsx        # 🏠 홈
│   │   ├── diary/
│   │   │   └── page.tsx        # 수면 일지 작성 폼
│   │   ├── records/
│   │   │   └── page.tsx        # 📊 기록 (서브탭 포함)
│   │   ├── prescription/
│   │   │   └── page.tsx        # 💊 처방
│   │   ├── isi/
│   │   │   └── page.tsx        # 📋 자가진단
│   │   └── qna/
│   │       ├── page.tsx        # 💬 문의 목록
│   │       └── [id]/
│   │           └── page.tsx    # 문의 상세
│   └── api/
│       ├── auth/
│       ├── diary/
│       ├── home/
│       ├── medication/
│       ├── records/
│       ├── prescriptions/
│       ├── isi/
│       ├── qna/
│       └── settings/
├── components/
│   ├── ui/                     # shadcn/ui 컴포넌트
│   ├── charts/                 # Recharts 래퍼
│   ├── forms/                  # 입력 폼 컴포넌트
│   └── layout/                 # 탭바, 헤더 등
├── lib/
│   ├── supabase.ts             # Supabase 클라이언트
│   ├── auth.ts                 # 인증 유틸
│   └── utils.ts                # 수면 효율 계산 등
└── types/
    └── index.ts                # TypeScript 타입 정의
```