# 관리자용 대시보드 화면별 상세 설계서

## 1. 개요

**문서 목적:** PRD(수면장애 환자 관리 시스템)의 관리자용 화면 요구사항을 **구현 가능한 수준의 화면 단위 명세**로 변환한 상세 설계서

**대상 플랫폼:** 데스크탑 우선 웹앱 (반응형)

**기술 스택:** Next.js + Tailwind CSS + shadcn/ui + Recharts + Supabase

### 사이드바 메뉴 구조 (7개)

| **아이콘** | **메뉴명** | **핵심 역할** |
| --- | --- | --- |
| 📊 | 대시보드 | 재택 관리 현황 요약, 미활동 환자 알림, 임박한 재방문 일정, 미답변 Q&A 수 |
| 👥 | 환자 목록 | 전체 환자 테이블, 검색·필터·정렬, 복약 순응도 집중 관리 |
| 🔍 | 환자 상세 | 개별 환자 수면 일지 그래프, 복약 이력, 증상 변화, 수면 일지 수정 |
| ✏️ | 환자 등록/수정 | 신규 환자 등록, 진단명·중증도·처방 정보 입력 및 수정, 재방문 일정 설정 |
| 📋 | 수면 데이터 입력 | 웨어러블·PSG 측정값 기반 객관적 수면 데이터 직접 입력 |
| 🔬 | 검사 결과 입력 | HRV·InBody·QEEG 검사 수치 입력, 원장 코멘트 등록 |
| 💬 | Q&A 관리 | 전체 환자 문의 목록, 미답변 우선 필터, 답변 작성 및 등록 |

### 공통 UI 원칙

- **데스크탑 우선:** 넓은 레이아웃, 테이블 중심 UI, 멀티 패널 구성
- **색상 톤:** 화이트 배경 + 소프트 그레이 보더, 다크 네이비 사이드바, 소프트 블루 액센트
- **사이드바:** 좌측 고정 240px, 축소 시 아이콘 모드(64px)
- **테이블:** 인터랙티브 정렬·필터 즉시 반응, 행 클릭 시 상세 페이지 이동
- **차트:** Recharts 기반, 호버 시 툴팁 표시, 범례 포함
- **반응형:** 태블릿 이하에서는 사이드바 → 햄버거 메뉴 전환

---

## 2. 📊 대시보드

### 2.1 화면 구성 요소

```
┌──────────┬─────────────────────────────────────────────────────────┐
│          │  📊 대시보드                             🔔 알림 👤 관리자명  │
│  📊 대시보드 ├─────────────────────────────────────────────────────────┤
│  👥 환자목록│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ │
│  🔍 환자상세│  │ 📝 일지   │ │ 💊 복약   │ │ 📅 재방문  │ │ 💬 미답변 │ │
│  ✏️ 등록  │  │ 미작성 5명 │ │ 순응도↓3명│ │ 이번주 8명 │ │ Q&A 4건  │ │
│  📋 수면  │  └──────────┘ └──────────┘ └──────────┘ └──────────┘ │
│  🔬 검사  ├─────────────────────────────────────────────────────────┤
│  💬 Q&A  │  ⚠️ 미활동 환자 알림                                    │
│          │  ┌───────────────────────────────────────────────────┐ │
│          │  │ 🔴 이영희 — 수면 일지 14일 미작성                       │ │
│          │  │ 🟡 홍길동 — 수면 일지 7일 미작성                        │ │
│          │  │ 🟡 김철수 — 복약 체크 5일 미작성 (처방 환자)              │ │
│          │  └───────────────────────────────────────────────────┘ │
│          ├─────────────────────────────────────────────────────────┤
│          │  📅 이번 주 재방문 예정      │ 📉 수면 효율 저하 환자    │
│          │  ┌────────────────────────┐ │ ┌────────────────────┐  │
│          │  │ 4/24 홍길동 (경과 확인)  │ │ │ 김철수  62% 🔴      │  │
│          │  │ 4/25 박민수 (처방 갱신)  │ │ │ 이영희  71% 🟡      │  │
│          │  │ 4/26 이영희 (초진 후 1주)│ │ │ 박민수  68% 🔴      │  │
│          │  └────────────────────────┘ │ └────────────────────┘  │
└──────────┴─────────────────────────────────────────────────────────┘
```

### 2.2 카드 지표 상세

| **카드명** | **표시 데이터** | **산출 로직** |
| --- | --- | --- |
| 📝 일지 미작성 | 최근 3일간 수면 일지 미작성 환자 수 | `sleep_diary`에서 최근 3일 내 `diary_date` 레코드 없는 환자 카운트 |
| 💊 복약 순응도↓ | 최근 7일 복약 순응도 70% 미만 환자 수 (처방 환자 한정) | 처방 활성 환자 중 `sleep_diary` 복약 컬럼 체크율 70% 미만 |
| 📅 재방문 임박 | 이번 주 내 재방문 예약 환자 수 | `treatment_records.next_visit_date` BETWEEN 이번 주 시작 AND 이번 주 끝 |
| 💬 미답변 Q&A | 미답변 문의 건수 | `qna.is_answered = false` 카운트 |

### 2.3 미활동 환자 알림 규칙

| **조건** | **알림 메시지** | **우선순위** |
| --- | --- | --- |
| 수면 일지 14일 이상 미작성 | "{환자명} — 수면 일지 {N}일 미작성" | 🔴 긴급 |
| 수면 일지 7일 이상 미작성 | "{환자명} — 수면 일지 {N}일 미작성" | 🟡 주의 |
| 처방 환자 복약 체크 5일 이상 없음 | "{환자명} — 복약 체크 {N}일 미작성" | 🟡 주의 |

### 2.4 데이터 흐름

| **UI 요소** | **Supabase 테이블** | **주요 컬럼 / 조건** |
| --- | --- | --- |
| 일지 미작성 카드 | `sleep_diary`, `patients` | 최근 3일 내 `diary_date` 레코드 없는 환자 JOIN |
| 복약 순응도 카드 | `sleep_diary`, `treatment_records`, `patients` | 활성 처방 환자 필터 → 최근 7일 복약 체크율 집계 |
| 재방문 임박 카드 | `treatment_records`, `patients` | `next_visit_date` BETWEEN 이번 주 시작 AND 이번 주 끝 |
| 미답변 Q&A 카드 | `qna` | `is_answered = false` 카운트 |
| 미활동 환자 알림 | `sleep_diary`, `patients` | 마지막 `diary_date`로부터 경과일 계산 |
| 수면 효율 저하 환자 | `sleep_diary`, `patients` | 최근 7일 평균 수면 효율 85% 미만 환자 |

### 2.5 API 엔드포인트

| **Method** | **Path** | **설명** |
| --- | --- | --- |
| `GET` | `/api/admin/dashboard/summary` | 4개 카드 지표 통합 조회 (일지 미작성·복약 순응도↓·재방문 임박·미답변 Q&A) |
| `GET` | `/api/admin/dashboard/inactive` | 미활동 환자 알림 목록 (일지 미작성·복약 미체크 기준, 우선순위 정렬) |
| `GET` | `/api/admin/dashboard/visits?range=week` | 이번 주 재방문 예정 환자 목록 |
| `GET` | `/api/admin/dashboard/low-efficiency` | 수면 효율 저하 환자 목록 (최근 7일 평균 85% 미만) |

### 2.6 UI 상태 처리

- **로딩:** 카드 4개 스켈레톤 + 리스트 영역 스피너
- **데이터 없음(미활동):** "모든 환자가 정상적으로 기록 중입니다 ✅"
- **데이터 없음(재방문):** "이번 주 재방문 예정 환자가 없습니다."
- **에러:** 토스트 메시지 "대시보드 데이터를 불러올 수 없습니다. 다시 시도해 주세요."

---

## 3. 👥 환자 목록

### 3.1 화면 구성 요소

```
┌─────────────────────────────────────────────────────────────────┐
│  👥 환자 목록                                    [+ 신규 환자 등록]  │
├─────────────────────────────────────────────────────────────────┤
│  🔍 검색: [등록번호, 이름, 진단명________]                          │
│  필터: [전체 ▼] [처방 환자만 ▼] [미활동 환자 ▼]                     │
├────┬──────┬──────┬──────────┬────────┬──────┬──────┬────────────┤
│ #  │ 등록번호│ 이름  │ 진단명    │ 중증도  │최근일지│복약순응│ 다음 방문일   │
├────┼──────┼──────┼──────────┼────────┼──────┼──────┼────────────┤
│ 1  │ P001 │ 홍길동 │ 불면증    │ 중등도  │ 4/22 │ 85% │ 4/24       │
│ 2  │ P002 │ 김철수 │ 불면증    │ 경미   │ 4/18 │ 62%🔴│ 4/30      │
│ 3  │ P003 │ 이영희 │ 수면무호흡│ 심각   │ 4/10 │  —  │ 4/25       │
├────┴──────┴──────┴──────────┴────────┴──────┴──────┴────────────┤
│                    < 1  2  3 ... 10 >                           │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 테이블 컬럼 상세

| **컬럼명** | **데이터 소스** | **정렬** | **비고** |
| --- | --- | --- | --- |
| 등록번호 | `patients.registration_number` | ✅ | 클릭 시 환자 상세 페이지 이동 |
| 이름 | `patients.name` | ✅ | 클릭 시 환자 상세 페이지 이동 |
| 진단명 | `sleep_disorders.diagnosis` | ✅ | 복수 진단 시 쉼표 구분 |
| 중증도 | `sleep_disorders.severity` | ✅ | 경미 🟢 / 중등도 🟡 / 심각 🔴 |
| 최근 일지 | `sleep_diary.diary_date` MAX | ✅ | 7일 이상 미작성 시 🟡, 14일 이상 🔴 |
| 복약 순응 | `sleep_diary` 복약 컬럼 집계 | ✅ | 처방 환자만 표시, 비처방 환자는 "—" |
| 다음 방문일 | `treatment_records.next_visit_date` | ✅ | 3일 이내 임박 시 🟡 강조 |

### 3.3 필터 옵션

| **필터명** | **옵션** | **로직** |
| --- | --- | --- |
| 상태 필터 | 전체 / 활성 / 비활성 | 비활성 = 14일 이상 일지 미작성 |
| 처방 필터 | 전체 / 처방 환자만 / 비처방 환자만 | `treatment_records`에 활성 처방 존재 여부 |
| 미활동 필터 | 전체 / 일지 7일+ 미작성 / 복약 5일+ 미체크 | 경과일 기반 필터 |

### 3.4 데이터 흐름

| **UI 요소** | **Supabase 테이블** | **주요 컬럼 / 조건** |
| --- | --- | --- |
| 환자 테이블 | `patients` JOIN `sleep_disorders` | `registration_number`, `name`, `diagnosis`, `severity` |
| 최근 일지일 | `sleep_diary` | 환자별 MAX(`diary_date`) |
| 복약 순응도 | `sleep_diary`, `treatment_records` | 활성 처방 기간 내 복약 체크율 계산 |
| 다음 방문일 | `treatment_records` | 최신 레코드의 `next_visit_date` |

### 3.5 API 엔드포인트

| **Method** | **Path** | **설명** |
| --- | --- | --- |
| `GET` | `/api/admin/patients?page=1&limit=20&search=&filter=` | 환자 목록 조회 (페이지네이션·검색·필터·정렬). 각 환자에 최근 일지일·복약 순응도·다음 방문일 집계 포함 |
| `GET` | `/api/admin/patients/export?format=csv` | 전체 환자 목록 CSV 내보내기 |

### 3.6 UI 상태 처리

- **로딩:** 테이블 행 5줄 스켈레톤
- **검색 결과 없음:** "검색 조건에 맞는 환자가 없습니다."
- **환자 0명:** "등록된 환자가 없습니다. '신규 환자 등록' 버튼을 눌러 환자를 추가해 주세요."
- **행 클릭:** 해당 환자 상세 페이지로 이동 (`/admin/patients/:id`)

---

## 4. 🔍 환자 상세

### 4.1 화면 구성 요소 (2단 레이아웃)

```
┌──────────────────────┬──────────────────────────────────────────┐
│  ← 환자 목록    🔍 홍길동 (P001)                  [수정] [삭제]     │
├──────────────────────┼──────────────────────────────────────────┤
│  📋 기본 정보          │  [수면] [효율] [복약] [검사] [ISI]          │
│  ┌──────────────────┐│  ┌──────────────────────────────────────┐│
│  │ 등록번호: P001    ││  │                                      ││
│  │ 이름: 홍길동      ││  │     (선택된 서브탭에 따라 변경)          ││
│  │ 생년월일: 1985.03 ││  │                                      ││
│  │ 연락처: 010-xxxx  ││  │  수면 패턴 꺾은선 그래프 (30일)          ││
│  ├──────────────────┤│  │                                      ││
│  │ 진단: 불면증       ││  │                                      ││
│  │ 중증도: 중등도     ││  └──────────────────────────────────────┘│
│  │ 발병일: 2025.01   ││  ┌──────────────────────────────────────┐│
│  ├──────────────────┤│  │ 📝 수면 일지 상세 (날짜별 테이블)        ││
│  │ 현재 처방:        ││  │ 날짜 | 취침 | 기상 | 만족도 | 효율      ││
│  │ 가감귀비탕        ││  │ 4/22 | 23:30| 6:00 | ★★★ | 82%      ││
│  │ 4/1 ~ 4/28       ││  │ 4/21 | 00:00| 6:30 | ★★  | 75%      ││
│  ├──────────────────┤│  └──────────────────────────────────────┘│
│  │ 다음 방문: 4/24   ││                                           │
│  │ 최근 ISI: 18(중등)││                                           │
│  └──────────────────┘│                                           │
└──────────────────────┴──────────────────────────────────────────┘
```

### 4.2 좌측 패널: 환자 요약 정보

| **항목** | **Supabase 테이블** | **주요 컬럼** |
| --- | --- | --- |
| 기본 정보 | `patients` | `registration_number`, `name`, `birth_date`, `phone` |
| 진단 정보 | `sleep_disorders` | `diagnosis`, `severity`, `onset_date`, `notes` |
| 현재 처방 | `treatment_records` | 최신 레코드의 `prescription`, `visit_date`, `next_visit_date` |
| 최근 ISI | `isi_assessments` | 최신 `total_score`  • 단계 해석 |

### 4.3 우측 패널: 서브탭 상세

**① 수면 서브탭**

- 수면 패턴 꺾은선 그래프 (30일 기본, 기간 선택 가능): 취침시간·기상시간·총수면시간
- 수면 단계 스택 바 차트: 관리자 입력 데이터 기반 (총 수면/깊은 수면/얕은 수면/REM)
- 수면 만족도·컨디션 추이 그래프
- 날짜별 수면 일지 상세 테이블 (행 클릭 시 수정 가능)

**② 효율 서브탭**

- 수면 효율(%) 꺾은선 그래프 (30일/90일 기간 선택)
- 기준선 표시: 85%(정상 경계), 70%(불량 경계) 수평선
- 색상 코딩: 85% 이상 🟢 / 85% 미만 🟡 / 70% 미만 🔴
- 평균 수면 효율 카드: 최근 7일 / 30일 평균

**③ 복약 서브탭** (처방 환자만)

- 복약 순응도 캘린더: 날짜별 한약·양약 체크 현황 히트맵
- 순응도 추이 꺾은선 그래프 (주 단위 %)
- 기간별 순응도 요약 카드: 최근 7일 / 30일

**④ 검사 서브탭**

- HRV / QEEG / InBody 하위 탭 3개
- 각 검사별 날짜 역순 카드 리스트 + 주요 수치 추이 그래프
- 원장 코멘트 표시

**⑤ ISI 서브탭**

- ISI 점수 꺾은선 그래프 (전체 이력)
- 기준선: 7 / 14 / 21
- 각 진단 날짜별 상세 점수 (Q1~Q7) 테이블

### 4.4 수면 일지 수정 기능

| **항목** | **동작** | **비고** |
| --- | --- | --- |
| 수정 진입 | 날짜별 일지 행 클릭 → 모달 팝업 | 기존 데이터 프리필 |
| 수정 가능 항목 | 수면 일지 전체 항목 (Step 1~3) | 복약 체크 포함 |
| 수정 이력 기록 | `updated_by`, `updated_at` 자동 저장 | 감사 로그 보존, 환자에게 미노출 |
| 수정 확인 | "수정하시겠습니까?" 확인 다이얼로그 | 실수 방지 |

### 4.5 데이터 흐름

| **UI 요소** | **Supabase 테이블** | **주요 컬럼 / 조건** |
| --- | --- | --- |
| 환자 요약 패널 | `patients`, `sleep_disorders`, `treatment_records`, `isi_assessments` | 환자 ID 기반 JOIN 조회 |
| 수면 차트 | `sleep_diary` | `diary_date`, `bedtime`, `wake_time`, `sleep_quality`, `condition`, `total_sleep_min`, `deep_sleep_min`, `light_sleep_min`, `rem_sleep_min` |
| 복약 캘린더 | `sleep_diary` | `herbal_*`, `western_*` 컬럼 (처방 기간 내) |
| 검사 결과 | `exam_results` | `exam_type` 필터, `result_data`, `summary` |
| ISI 이력 | `isi_assessments` | `assessed_at`, `q1`~`q7`, `total_score` |
| 일지 수정 | `sleep_diary` | 전체 컬럼 UPDATE + `updated_by`, `updated_at` |

### 4.6 API 엔드포인트

| **Method** | **Path** | **설명** |
| --- | --- | --- |
| `GET` | `/api/admin/patients/:id` | 환자 상세 정보 (기본 정보 + 진단 + 현재 처방 + 최근 ISI) |
| `GET` | `/api/admin/patients/:id/sleep?range=30d|90d` | 수면 일지 기록 목록 (차트 + 테이블용) |
| `GET` | `/api/admin/patients/:id/efficiency?range=30d|90d` | 날짜별 수면 효율 배열 |
| `GET` | `/api/admin/patients/:id/medication?range=30d|90d` | 복약 체크 이력 (캘린더·순응도 계산용) |
| `GET` | `/api/admin/patients/:id/exams/:type` | 검사 결과 목록 (type: hrv, qeeg, inbody) |
| `GET` | `/api/admin/patients/:id/isi` | ISI 이력 전체 조회 |
| `PUT` | `/api/admin/diary/:diaryId` | 수면 일지 수정 (관리자). `updated_by`, `updated_at` 자동 기록 |

### 4.7 UI 상태 처리

- **로딩:** 좌측 패널 스켈레톤 + 우측 차트 영역 스피너
- **환자 없음(잘못된 ID):** 404 — "환자를 찾을 수 없습니다." + 목록으로 돌아가기 버튼
- **수면 데이터 없음:** "아직 수면 일지 기록이 없습니다."
- **관리자 수면 데이터 없음:** 수면 단계 스택 바 차트 영역 숨김
- **검사 결과 없음:** "등록된 {검사 유형} 검사 결과가 없습니다."
- **일지 수정 성공:** 토스트 "수면 일지가 수정되었습니다." + 차트·테이블 자동 갱신

---

## 5. ✏️ 환자 등록/수정

### 5.1 화면 구성 요소

```
┌─────────────────────────────────────────────────────────────────┐
│  ✏️ 신규 환자 등록  (수정 시: "환자 정보 수정")                       │
├─────────────────────────────────────────────────────────────────┤
│  📋 기본 정보                                                     │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ 등록번호*:    [P004_____________]  (직접 입력, 중복 검사)    │  │
│  │ 이름*:       [________________]                           │  │
│  │ 생년월일:     [____년 __월 __일] (날짜 피커)                │  │
│  │ 연락처:      [010-____-____]                              │  │
│  └───────────────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────────────┤
│  🩺 진단 정보                                                     │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ 진단명*:     [불면증__________ ▼]  (선택 또는 직접 입력)   │  │
│  │ 중증도:      (○) 경미  (●) 중등도  (○) 심각               │  │
│  │ 발병일:      [____년 __월 __일]                           │  │
│  │ 비고:        [____________________________]              │  │
│  └───────────────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────────────┤
│  💊 처방 정보                                                     │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ 처방명:      [가감귀비탕________]                           │  │
│  │ 복약 지시:   [아침 식후 30분 1포, 저녁 식후 30분 1포______]  │  │
│  │ 처방 시작일: [____년 __월 __일]                            │  │
│  │ 처방 종료일: [____년 __월 __일]                            │  │
│  │ 진료 메모:   [____________________________]              │  │
│  └───────────────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────────────┤
│  📅 재방문 일정                                                   │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ 다음 방문 예정일: [____년 __월 __일]                        │  │
│  │ 방문 목적:       [경과 확인 ▼]                             │  │
│  └───────────────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────────────┤
│                    [취소]        [저장]                           │
└─────────────────────────────────────────────────────────────────┘
```

### 5.2 입력 필드 상세

| **필드명** | **필수** | **입력 타입** | **유효성 검사** | **비고** |
| --- | --- | --- | --- | --- |
| 등록번호 | ✅ 필수 | 텍스트 | 고유값, 최대 20자 | 수정 불가 (등록 후 고정) |
| 이름 | ✅ 필수 | 텍스트 | 최대 100자 |  |
| 생년월일 | 선택 | 날짜 피커 | 미래 날짜 불가 |  |
| 연락처 | 선택 | 전화번호 | 형식 검증 | 010-XXXX-XXXX |
| 진단명 | ✅ 필수 | 셀렉트 + 직접입력 | — | 불면증, 수면무호흡증, 기면병, 하지불안증후군 등 프리셋 |
| 중증도 | 선택 | 라디오 버튼 | 경미/중등도/심각 |  |
| 처방명 | 선택 | 텍스트 | 최대 200자 | 처방 없으면 빈 값 |
| 복약 지시 | 선택 | 텍스트 | — | 처방 입력 시 함께 기록 |
| 다음 방문 예정일 | 선택 | 날짜 피커 | 오늘 이후 날짜 |  |

### 5.3 데이터 흐름

| **UI 요소** | **Supabase 테이블** | **읽기 컬럼** | **쓰기 컬럼** |
| --- | --- | --- | --- |
| 기본 정보 폼 | `patients` | 수정 시 기존 데이터 프리필 | `registration_number`, `name`, `birth_date`, `phone` |
| 진단 정보 폼 | `sleep_disorders` | 수정 시 기존 데이터 프리필 | `diagnosis`, `severity`, `onset_date`, `notes` |
| 처방 정보 폼 | `treatment_records` | 수정 시 최신 처방 프리필 | `prescription`, `treatment_notes`, `visit_date`, `next_visit_date` |
| 계정 생성 | `user_roles`, Supabase Auth | — | `role='patient'`, `patient_id` 연결 |

### 5.4 API 엔드포인트

| **Method** | **Path** | **설명** |
| --- | --- | --- |
| `POST` | `/api/admin/patients` | 신규 환자 등록. `patients`  • `sleep_disorders`  • `treatment_records` 동시 생성. 등록번호 중복 시 409 에러 |
| `PUT` | `/api/admin/patients/:id` | 환자 정보 수정 (기본 정보 + 진단 정보) |
| `POST` | `/api/admin/patients/:id/prescription` | 새 처방 추가 (기존 처방 이력 보존) |
| `PUT` | `/api/admin/patients/:id/prescription/:prescId` | 처방 정보 수정 |
| `DELETE` | `/api/admin/patients/:id` | 환자 삭제 (소프트 삭제 권장, 확인 다이얼로그 필수) |
| `GET` | `/api/admin/patients/check-registration/:regNo` | 등록번호 중복 확인 (실시간 검증) |

### 5.5 UI 상태 처리

- **등록번호 중복:** 입력 필드 하단에 "이미 사용 중인 등록번호입니다." 빨간색 안내
- **필수 입력 누락:** 미입력 필드 강조 + "필수 항목을 입력해 주세요." 안내
- **저장 성공(등록):** 토스트 "환자가 등록되었습니다." + 환자 상세 페이지로 이동
- **저장 성공(수정):** 토스트 "환자 정보가 수정되었습니다."
- **삭제 확인:** "정말 삭제하시겠습니까? 이 작업은 되돌릴 수 없습니다." 확인 다이얼로그

---

## 6. 📋 수면 데이터 입력

### 6.1 화면 구성 요소

```
┌─────────────────────────────────────────────────────────────────┐
│  📋 수면 데이터 입력 (웨어러블/PSG 측정값)                           │
├─────────────────────────────────────────────────────────────────┤
│  환자 선택:  [🔍 등록번호 또는 이름으로 검색_____________ ▼]          │
│  측정 날짜:  [____년 __월 __일]                                   │
├─────────────────────────────────────────────────────────────────┤
│  🛏️ 수면 단계별 시간                                              │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ 총 수면 시간:   [__]시간 [__]분                             │  │
│  │ 깊은 수면 시간: [__]시간 [__]분  (Non-REM 3단계)            │  │
│  │ 얕은 수면 시간: [__]시간 [__]분  (Non-REM 1~2단계)          │  │
│  │ REM 수면 시간:  [__]시간 [__]분                             │  │
│  └───────────────────────────────────────────────────────────┘  │
│  ⚠️ 합산 불일치 경고: 깊은+얕은+REM ≠ 총 수면 시 노란색 경고 표시    │
├─────────────────────────────────────────────────────────────────┤
│  📝 관리자 메모                                                   │
│  [_______________________________________________________]      │
├─────────────────────────────────────────────────────────────────┤
│                    [취소]        [저장]                           │
└─────────────────────────────────────────────────────────────────┘
```

### 6.2 데이터 흐름

| **UI 요소** | **Supabase 테이블** | **읽기 컬럼** | **쓰기 컬럼** |
| --- | --- | --- | --- |
| 환자 선택 검색 | `patients` | `registration_number`, `name` | — |
| 수면 단계 입력 | `sleep_diary` | 기존 데이터 (있으면 프리필) | `total_sleep_min`, `deep_sleep_min`, `light_sleep_min`, `rem_sleep_min`, `admin_note` |

### 6.3 입력 검증 규칙

- **시간·분 변환:** 시간·분 분리 입력 → DB 저장 시 분 단위로 변환 (예: 6시간 30분 → 390분)
- **합산 검증:** 깊은 수면 + 얕은 수면 + REM 수면 합이 총 수면 시간과 ±15분 이상 차이 시 노란색 경고 (저장은 허용)
- **중복 입력 방지:** 동일 환자 + 동일 날짜에 기존 데이터 존재 시 "기존 데이터를 덮어쓰겠습니까?" 확인

### 6.4 API 엔드포인트

| **Method** | **Path** | **설명** |
| --- | --- | --- |
| `POST` | `/api/admin/sleep-data` | 수면 데이터 입력 `{ patient_id, diary_date, total_sleep_min, deep_sleep_min, light_sleep_min, rem_sleep_min, admin_note }` |
| `GET` | `/api/admin/sleep-data/:patientId/:date` | 특정 환자·날짜의 기존 데이터 조회 (중복 확인용) |
| `PUT` | `/api/admin/sleep-data/:patientId/:date` | 기존 수면 데이터 수정 |

### 6.5 UI 상태 처리

- **저장 성공:** 토스트 "수면 데이터가 저장되었습니다. 환자 앱 기록 탭에 즉시 반영됩니다."
- **합산 불일치 경고:** 노란색 인라인 경고 "수면 단계 합산이 총 수면 시간과 차이가 있습니다. 확인 후 저장해 주세요."
- **중복 데이터:** "이미 {날짜} 데이터가 존재합니다. 덮어쓰시겠습니까?" 확인 다이얼로그

---

## 7. 🔬 검사 결과 입력

### 7.1 화면 구성 요소

```
┌─────────────────────────────────────────────────────────────────┐
│  🔬 검사 결과 입력                                                │
├─────────────────────────────────────────────────────────────────┤
│  환자 선택:  [🔍 등록번호 또는 이름으로 검색_____________ ▼]          │
│  검사 유형:  (●) HRV   (○) QEEG   (○) InBody                    │
│  검사 날짜:  [____년 __월 __일]                                   │
├─────────────────────────────────────────────────────────────────┤
│  📊 검사 항목별 수치  (검사 유형에 따라 동적 변경)                    │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  [HRV 선택 시]                                             │  │
│  │  SDNN (ms):        [______]                               │  │
│  │  RMSSD (ms):       [______]                               │  │
│  │  LF/HF ratio:      [______]                               │  │
│  │  Total Power:      [______]                               │  │
│  │  + 항목 추가                                               │  │
│  └───────────────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────────────┤
│  📝 원장 코멘트 (해석 메모)                                       │
│  [_______________________________________________________]      │
├─────────────────────────────────────────────────────────────────┤
│                    [취소]        [저장]                           │
└─────────────────────────────────────────────────────────────────┘
```

### 7.2 검사 유형별 기본 항목

| **검사 유형** | **기본 제공 항목 (추가 입력 가능)** |
| --- | --- |
| HRV | SDNN (ms), RMSSD (ms), LF/HF ratio, Total Power, pNN50 (%) |
| QEEG | Delta (%), Theta (%), Alpha (%), Beta (%), Gamma (%), 주요 뇌파 이상 부위 |
| InBody | 체중 (kg), 골격근량 (kg), 체지방량 (kg), 체지방률 (%), BMI, 기초대사량 (kcal) |

> 항목별 수치는 `result_data JSONB`에 유연하게 저장. "+ 항목 추가" 버튼으로 비정형 항목도 입력 가능.
> 

### 7.3 데이터 흐름

| **UI 요소** | **Supabase 테이블** | **읽기 컬럼** | **쓰기 컬럼** |
| --- | --- | --- | --- |
| 환자 선택 검색 | `patients` | `registration_number`, `name` | — |
| 검사 수치 입력 | `exam_results` | 기존 데이터 (수정 시 프리필) | `patient_id`, `exam_date`, `exam_type`, `result_data`, `summary`, `created_by` |

### 7.4 API 엔드포인트

| **Method** | **Path** | **설명** |
| --- | --- | --- |
| `POST` | `/api/admin/exams` | 검사 결과 등록 `{ patient_id, exam_date, exam_type, result_data, summary }` |
| `PUT` | `/api/admin/exams/:id` | 검사 결과 수정 |
| `DELETE` | `/api/admin/exams/:id` | 검사 결과 삭제 (확인 다이얼로그 필수) |
| `GET` | `/api/admin/exams/:patientId?type=hrv|qeeg|inbody` | 특정 환자의 검사 결과 목록 조회 |

### 7.5 UI 상태 처리

- **저장 성공:** 토스트 "검사 결과가 등록되었습니다. 환자 앱 기록 탭에 즉시 반영됩니다."
- **검사 유형 전환:** 항목 폼 동적 교체, 기입 중인 데이터 초기화 전 확인 안내

---

## 8. 💬 Q&A 관리

### 8.1 화면 구성 요소

```
┌─────────────────────────────────────────────────────────────────┐
│  💬 Q&A 관리                                                     │
├─────────────────────────────────────────────────────────────────┤
│  필터: (●) 전체  (○) 미답변 우선  (○) 답변 완료                     │
│  🔍 검색: [환자명 또는 문의 내용________________]                   │
├──────┬──────────┬────────────────────────────┬─────────┬───────┤
│ 상태  │ 환자명    │ 문의 내용 (미리보기)          │ 문의일  │ 답변일 │
├──────┼──────────┼────────────────────────────┼─────────┼───────┤
│ 🟡미답변│ 홍길동  │ 약 복용 시간 조정이 가능한가요?  │ 4/20   │  —   │
│ 🟡미답변│ 김철수  │ 수면 중 경련이 발생했습니다      │ 4/21   │  —   │
│ 🟢완료 │ 이영희  │ 처방 기간 연장이 가능한가요?    │ 4/18   │ 4/19  │
├──────┴──────────┴────────────────────────────┴─────────┴───────┤
│  [선택된 문의 상세 + 답변 작성 영역]                                │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ Q: 약 복용 시간 조정이 가능한가요?                           │  │
│  │ 식전에 복용해도 되는지 여쭤봅니다.                           │  │
│  │ ──────────────────────────────                           │  │
│  │ A: [답변 작성___________________________________]         │  │
│  │                                      [답변 등록]          │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

### 8.2 데이터 흐름

| **UI 요소** | **Supabase 테이블** | **읽기 컬럼** | **쓰기 컬럼** |
| --- | --- | --- | --- |
| 문의 목록 | `qna` JOIN `patients` | `id`, `question`, `is_answered`, `created_at`, `patients.name` | — |
| 문의 상세 | `qna` | `question`, `answer`, `answered_at` | — |
| 답변 등록 | `qna` | — | `answer`, `is_answered`, `answered_by`, `answered_at` |

### 8.3 API 엔드포인트

| **Method** | **Path** | **설명** |
| --- | --- | --- |
| `GET` | `/api/admin/qna?status=all|pending|answered&search=` | 전체 문의 목록 조회 (상태 필터·검색·페이지네이션). 환자명 JOIN 포함 |
| `GET` | `/api/admin/qna/:id` | 문의 상세 조회 (질문 + 답변 + 환자 정보) |
| `POST` | `/api/admin/qna/:id/answer` | 답변 등록 `{ answer: string }` → `is_answered=true`, `answered_by`, `answered_at` 자동 기록. 환자에게 알림 발송 (알림 ON 시) |
| `PUT` | `/api/admin/qna/:id/answer` | 기존 답변 수정 |

### 8.4 UI 상태 처리

- **미답변 0건:** "미답변 문의가 없습니다. 모든 문의에 답변이 완료되었습니다 ✅"
- **답변 등록 성공:** 토스트 "답변이 등록되었습니다." + 해당 행 상태 🟢 완료로 전환
- **알림 발송 실패:** 토스트 "답변은 등록되었으나 알림 발송에 실패했습니다."
- **검색 결과 없음:** "검색 조건에 맞는 문의가 없습니다."

---

## 9. 인증 플로우

### 9.1 관리자 로그인 화면

```
┌─────────────────────────────┐
│  🏥 수면장애 클리닉             │
│     관리자 로그인               │
│                             │
│  이메일: [____________]       │
│  비밀번호: [____________]     │
│                             │
│  [       로그인       ]       │
│                             │
│  ──── 2단계 인증 ────          │
│  OTP 코드: [______]          │
│  [     확인     ]            │
└─────────────────────────────┘
```

### 9.2 인증 API

| **Method** | **Path** | **설명** |
| --- | --- | --- |
| `POST` | `/api/auth/admin/login` | 관리자 로그인 `{ email, password }` → JWT 반환 + 2FA OTP 발송 |
| `POST` | `/api/auth/admin/verify-otp` | OTP 검증 `{ otp }` → 최종 액세스 토큰 발급 |
| `POST` | `/api/auth/admin/logout` | 로그아웃 (서버 측 토큰 무효화) |
| `GET` | `/api/auth/admin/me` | 현재 로그인 관리자 정보 조회 |

### 9.3 보안 규칙

- JWT 액세스 토큰: **1시간** 만료
- 리프레시 토큰: **7일** 만료
- 비활동 **15분** 후 자동 로그아웃
- 로그인 실패 **5회** 시 10분 잠금
- 모든 API: `Authorization: Bearer {token}` 필수
- RLS: 관리자 role 확인 미들웨어 (`user_roles.role = 'admin'`)
- 2FA: OTP 또는 이메일 인증 코드 추가 검증 (권장)

---

## 10. 전체 API 엔드포인트 요약

| **Method** | **Path** | **화면** | **인증** |
| --- | --- | --- | --- |
| `POST` | `/api/auth/admin/login` | 로그인 | ❌ |
| `POST` | `/api/auth/admin/verify-otp` | 로그인 | ❌ |
| `POST` | `/api/auth/admin/logout` | 공통 | ✅ |
| `GET` | `/api/auth/admin/me` | 공통 | ✅ |
| `GET` | `/api/admin/dashboard/summary` | 📊 대시보드 | ✅ |
| `GET` | `/api/admin/dashboard/inactive` | 📊 대시보드 | ✅ |
| `GET` | `/api/admin/dashboard/visits` | 📊 대시보드 | ✅ |
| `GET` | `/api/admin/dashboard/low-efficiency` | 📊 대시보드 | ✅ |
| `GET` | `/api/admin/patients` | 👥 환자 목록 | ✅ |
| `GET` | `/api/admin/patients/export` | 👥 환자 목록 | ✅ |
| `GET` | `/api/admin/patients/:id` | 🔍 환자 상세 | ✅ |
| `GET` | `/api/admin/patients/:id/sleep` | 🔍 환자 상세 | ✅ |
| `GET` | `/api/admin/patients/:id/efficiency` | 🔍 환자 상세 | ✅ |
| `GET` | `/api/admin/patients/:id/medication` | 🔍 환자 상세 | ✅ |
| `GET` | `/api/admin/patients/:id/exams/:type` | 🔍 환자 상세 | ✅ |
| `GET` | `/api/admin/patients/:id/isi` | 🔍 환자 상세 | ✅ |
| `PUT` | `/api/admin/diary/:diaryId` | 🔍 환자 상세 | ✅ |
| `POST` | `/api/admin/patients` | ✏️ 등록/수정 | ✅ |
| `PUT` | `/api/admin/patients/:id` | ✏️ 등록/수정 | ✅ |
| `DELETE` | `/api/admin/patients/:id` | ✏️ 등록/수정 | ✅ |
| `POST` | `/api/admin/patients/:id/prescription` | ✏️ 등록/수정 | ✅ |
| `GET` | `/api/admin/patients/check-registration/:regNo` | ✏️ 등록/수정 | ✅ |
| `POST` | `/api/admin/sleep-data` | 📋 수면 데이터 | ✅ |
| `GET` | `/api/admin/sleep-data/:patientId/:date` | 📋 수면 데이터 | ✅ |
| `PUT` | `/api/admin/sleep-data/:patientId/:date` | 📋 수면 데이터 | ✅ |
| `POST` | `/api/admin/exams` | 🔬 검사 결과 | ✅ |
| `PUT` | `/api/admin/exams/:id` | 🔬 검사 결과 | ✅ |
| `DELETE` | `/api/admin/exams/:id` | 🔬 검사 결과 | ✅ |
| `GET` | `/api/admin/exams/:patientId` | 🔬 검사 결과 | ✅ |
| `GET` | `/api/admin/qna` | 💬 Q&A 관리 | ✅ |
| `GET` | `/api/admin/qna/:id` | 💬 Q&A 관리 | ✅ |
| `POST` | `/api/admin/qna/:id/answer` | 💬 Q&A 관리 | ✅ |
| `PUT` | `/api/admin/qna/:id/answer` | 💬 Q&A 관리 | ✅ |

---

## 11. Next.js 파일 구조 (권장)

```jsx
src/
├── app/
│   ├── layout.tsx                  # 루트 레이아웃
│   ├── admin/
│   │   ├── login/
│   │   │   └── page.tsx            # 관리자 로그인
│   │   └── (dashboard)/            # 관리자 인증 필요 그룹
│   │       ├── layout.tsx          # 사이드바 포함 레이아웃
│   │       ├── dashboard/
│   │       │   └── page.tsx        # 📊 대시보드
│   │       ├── patients/
│   │       │   ├── page.tsx        # 👥 환자 목록
│   │       │   ├── new/
│   │       │   │   └── page.tsx    # ✏️ 신규 환자 등록
│   │       │   └── [id]/
│   │       │       ├── page.tsx    # 🔍 환자 상세
│   │       │       └── edit/
│   │       │           └── page.tsx # ✏️ 환자 수정
│   │       ├── sleep-data/
│   │       │   └── page.tsx        # 📋 수면 데이터 입력
│   │       ├── exams/
│   │       │   └── page.tsx        # 🔬 검사 결과 입력
│   │       └── qna/
│   │           └── page.tsx        # 💬 Q&A 관리
│   └── api/
│       ├── auth/admin/
│       ├── admin/
│       │   ├── dashboard/
│       │   ├── patients/
│       │   ├── diary/
│       │   ├── sleep-data/
│       │   ├── exams/
│       │   └── qna/
├── components/
│   ├── ui/                         # shadcn/ui 컴포넌트
│   ├── charts/                     # Recharts 래퍼
│   ├── admin/
│   │   ├── Sidebar.tsx             # 사이드바 내비게이션
│   │   ├── PatientTable.tsx        # 환자 목록 테이블
│   │   ├── PatientDetailPanel.tsx  # 환자 상세 좌측 패널
│   │   ├── SleepCharts.tsx         # 수면 차트 묶음
│   │   ├── MedicationCalendar.tsx  # 복약 캘린더
│   │   └── QnaAnswerForm.tsx       # Q&A 답변 폼
│   └── forms/                      # 공통 폼 컴포넌트
├── lib/
│   ├── supabase.ts                 # Supabase 클라이언트
│   ├── auth.ts                     # 인증 유틸 (관리자)
│   ├── sleep-utils.ts              # 수면 효율 계산 등
│   └── utils.ts                    # 공통 유틸
└── types/
    └── index.ts                    # TypeScript 타입 정의
```