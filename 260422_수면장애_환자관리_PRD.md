# 수면장애 환자 관리 시스템 PRD

## 1. 제품 개요

**제품명:** 수면장애 환자 관리 시스템

**소속:** 원광대학교 광주한방병원 수면장애 클리닉

**목적:** 외래 진료 이후 자택으로 귀가한 환자의 수면 상태 및 복약 여부를 지속적으로 모니터링하여 치료의 연속성을 확보하는 웹 기반 플랫폼. 병원 시스템을 대체하는 것이 아니라 재택 관리에 초점을 둠.

**대상 사용자:**

- **환자**: 개인별 등록번호를 기반으로 수면 일지 및 치료 기록 관리
- **관리자**: 전체 환자 데이터 모니터링, 통계 분석, Q&A 응대

---

## 2. 핵심 기능 요구사항

### 2.1 환자 관리 기능

- **환자 등록:** 병원 등록번호와 동일하게 사용. 관리자가 직접 입력하거나 부여하는 방식으로 설정 (자동 생성 없음)
- **개인 정보 관리:** 이름, 생년월일, 연락처 입력 (최소 정보만 수집)
- **진단 정보 입력:** 진단명, 중증도 등 진료 시 확인된 내용을 관리자가 등록 (병원 기록 보완용)
- **처방 정보 관리:** 한약 처방명, 복약 지시사항, 처방 기간 등 외래 처방 내역 기록
- **재택 수면 일지:** 환자가 직접 매일 수면 상태(취침 시간, 기상 시간, 수면의 질, 특이사항)를 기록
- **증상 변화 추적:** 진료 간 기간 동안의 증상 호전·악화 여부를 시계열로 기록
- **조건부 복약 관리:** 관리자가 처방 정보를 등록한 환자에게만 복약 체크 메뉴가 활성화됨. 처방이 없는 환자는 복약 체크 및 복약 관련 알림 대상에서 자동 제외
- **복약 순응도 체크:** 처방 환자에 한해 한약·양약 각각 아침·점심·저녁·취침전 복약 여부를 스스로 체크 입력
- **다음 방문 일정 확인:** 다음 외래 예약일 및 관리자가 설정한 재방문 안내 확인
- **알림 ON/OFF 설정:** 푸시 알림은 **기본 OFF**으로 설정되며, 환자가 원할 때 항목별(수면 일지 작성 리마인드, 복약 알림, Q&A 답변 알림)으로 독립적으로 ON 할 수 있음. 관리자는 별도로 설정 가능
- **검사 결과 열람:** 원장님(관리자)이 입력한 HRV, InBody, QEEG 검사 결과를 환자가 앱에서 날짜별로 확인
- **ISI 자가진단:** 앱 내 자가진단 탭에서 7문항을 직접 체크 및 제출. 제출 후 합산 점수와 증상 단계(없음/경미/중등도/심각)를 즉시 확인. 날짜별 점수 이력 그래프 제공
- **검색 및 필터링:** 등록번호, 이름, 진단명 등으로 환자 검색

### 2.2 관리자 대시보드

- **재택 관리 현황 요약:** 수면 일지 미작성 환자, 복약 순응도 저하 환자 **(처방 환자 한정)**, 증상 악화 환자를 한눈에 파악
- **환자별 수면 추이:** 환자가 입력한 수면 일지 데이터를 기반으로 수면 패턴 변화 시각화
- **재방문 일정 관리:** 다음 외래 예약이 임박한 환자 목록 및 방문 전 확인 필요 사항 표시
- **미활동 환자 알림:** 일정 기간 동안 수면 일지 미입력 또는 복약 체크 없는 환자 자동 알림
- **전체 환자 목록:** 등록 환자 테이블 (수면 일지 최근 입력일, 복약 순응도, 다음 방문일 등 포함)

### 2.4 Q&A (문의 및 답변)

- **문의 작성:** 환자가 궁금한 점, 증상 관련 질문 등을 텍스트로 작성하여 제출
- **답변 등록:** 관리자가 해당 문의에 답변 작성 및 등록
- **문의 목록 조회:** 환자는 본인의 문의 내역과 답변 상태(답변 대기·완료) 확인
- **알림:** 관리자가 답변을 등록하면 환자에게 알림 발송 **(알림 ON 설정 시에 한함)**
- **관리자 문의 관리:** 전체 환자 문의 목록을 확인하고 미답변 문의 우선 처리

### 2.5 수면 일지 세부 항목

> 환자가 매일 1회 모바일에서 입력하는 항목. 입력 부담을 최소화하기 위해 대부분 선택형으로 구성.
> 

**기본 수면 정보 (선택)**

- **취침 시간** — 어젯밤 잠자리에 든 시간 (시:분 피커 또는 직접 숫자 입력)
- **기상 시간** — 오늘 아침 최종 기상 시간 (시:분 피커 또는 직접 숫자 입력)
- **잠들기까지 걸린 시간** — 0~10분 / 10~30분 / 30~60분 / 60분 이상 (선택)
- **야간 각성 횟수** — 없음 / 1회 / 2회 / 3회 이상 (선택)
- **총 수면 시간** — 취침·기상 시간 기반 자동 계산 (참고 표시용)
- **수면 중 특이사항** — 자유 텍스트 (선택, 최대 200자 / 수면 중 특정 이벤트 발생 시 작성. 예: 가위눈림, 수면 중 경련, 코골이)

**수면의 질 평가 (선택)**

- **수면 만족도** — 1~5점 슬라이더 (1: 매우 나쁨, 5: 매우 좋음)
- **기상 시 피로도** — 1~5점 슬라이더 (1: 매우 피곤함, 5: 매우 개운함)
- **낮 동안 졸림** — 없음 / 약간 / 심함 (선택)

**추가 상태 기록 (선택)**

- **낮잠 여부** — 없음 / 있음 (있으면 시간대 및 시간 입력)
- **꿈** — 없음 / 기억 안 남 / 꿈을 꿈 (선택)
- **카페인 섭취** — 없음 / 오전 / 오후 / 저녁 (선택)
- **음주** — 없음 / 있음 (선택)
- **오늘 컨디션** — 1~5점 슬라이더 (1: 매우 나쁨, 5: 매우 좋음)
- **특이사항 메모** — 자유 텍스트 (선택, 최대 200자)

**복약 체크 (선택, 연동)**

- 수면 일지 입력 후 당일 복약 여부 함께 확인
- **한약:** 아침 / 점심 / 저녁 / 취침전 (4회)
- **양약:** 아침 / 점심 / 저녁 / 취침전 (4회)
- 한약·양약 처방이 없는 항목은 해당 섹션 자체가 비노출

**관리자 추가 입력 항목 (객관적 수면 데이터)**

> 웨어러블 기기, 수면 다원검사(PSG) 등 외부 측정값을 관리자가 직접 입력. 입력된 데이터는 **환자도 기록 탭에서 그래프로 확인 가능**.
> 
- **총 수면 시간** — 시간·분 분리 입력 (예: 6시간 0분)
- **깊은 수면 시간** — 시간·분 분리 입력 (Non-REM 3단계)
- **얕은 수면 시간** — 시간·분 분리 입력 (Non-REM 1~2단계)
- **REM 수면 시간** — 시간·분 분리 입력
- DB 저장 시 내부적으로 분 단위로 변환하여 저장 (예: 6시간 30분 → 390분)
- **조회 권한:** 관리자 입력 후 환자 앱 기록 탭에 즉시 노출. 수면 단계별 비율을 스택 바 차트로 시각화하여 제공.

### 2.6 수면 효율 분석

> 수면 효율(Sleep Efficiency) = 실제 수면 시간 ÷ 침대에 누워 있는 시간 × 100
> 
- **자동 계산:** 환자가 입력한 취침 시간·기상 시간·잠들기까지 걸린 시간·야간 각성 횟수를 기반으로 수면 효율(%) 자동 산출
- **기준 해석:** 85% 이상 정상 / 85% 미만 주의 / 70% 미만 불량으로 색상 코딩 표시
- **추이 그래프:** 날짜별 수면 효율 변화를 꺾은선 그래프로 시각화 (환자 기록 탭 + 관리자 환자 상세 화면)
- **관리자 활용:** 대시보드에서 수면 효율 저하 환자를 빠르게 파악할 수 있도록 지표 제공

### 2.3 인증 및 권한 관리

- **환자 로그인:** 등록번호 + 비밀번호
- **관리자 로그인:** 이메일 + 비밀번호 (2단계 인증 권장)
- **권한 분리:** 환자는 본인 정보만 조회/수정, 관리자는 모든 정보 접근 가능
- **수면 일지 수정 권한:** 관리자는 환자가 작성한 수면 일지 항목을 수정할 수 있음. 수정 시 `updated_by`(수정자 ID)·`updated_at`(수정 일시) 자동 기록. 수정 이력은 감사 로그에 보존되며 환자에게는 노출하지 않음.

---

## 3. 기술 스택 제안

### 3.0 UX/UI 디자인 방향

- **레퍼런스 수집:** [Dribbble](https://dribbble.com)에서 헬스케어·수면·모바일 앱 관련 디자인을 탐색하여 원하는 스타일을 선택
- **선택 기준:** 수면장애 클리닉 특성에 맞는 차분하고 신뢰감 있는 톤앤매너 (예: 다크 네이비·소프트 블루 계열), 가독성 높은 타이포그래피, 직관적인 차트·슬라이더 UI 우선
- **활용 방식:** 선택한 Dribbble 디자인을 참고하여 Figma 또는 직접 컴포넌트 구현 시 스타일 가이드로 활용. 저작권 있는 에셋은 직접 사용 불가이므로 스타일 참고 용도로만 사용
- **추천 검색 키워드:** `health app`, `sleep tracker`, `medical dashboard`, `patient app`

### 3.1 프론트엔드

- **프레임워크:** Next.js (React 기반, SEO 최적화)
- **UI 라이브러리:** Tailwind CSS, shadcn/ui
- **차트:** Recharts 또는 Chart.js
- **상태 관리:** Zustand 또는 Redux Toolkit

### 3.2 백엔드

- **데이터베이스:** Supabase (PostgreSQL 기반)
- **인증:** Supabase Auth
- **API:** Next.js API Routes 또는 Supabase Functions

### 3.3 배포

- **호스팅:** Vercel
- **도메인:** wku.sleepclinic 관련 도메인

---

## 4. 데이터베이스 스키마 (초안)

### 4.1 patients 테이블

```sql
CREATE TABLE patients (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  registration_number VARCHAR(20) UNIQUE NOT NULL, -- 병원 등록번호와 동일, 관리자가 직접 입력
  name VARCHAR(100) NOT NULL,
  birth_date DATE,
  phone VARCHAR(20),
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);
```

### 4.2 sleep_disorders 테이블

```sql
CREATE TABLE sleep_disorders (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  patient_id UUID REFERENCES patients(id),
  diagnosis VARCHAR(200),
  symptoms TEXT[],
  severity VARCHAR(20),
  onset_date DATE,
  notes TEXT,
  created_at TIMESTAMP DEFAULT NOW()
);
```

### 4.3 treatment_records 테이블

```sql
CREATE TABLE treatment_records (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  patient_id UUID REFERENCES patients(id),
  visit_date DATE NOT NULL,
  prescription TEXT,
  treatment_notes TEXT,
  next_visit_date DATE,
  created_by UUID REFERENCES auth.users(id),
  created_at TIMESTAMP DEFAULT NOW()
);
```

### 4.4 sleep_diary 테이블

```sql
CREATE TABLE sleep_diary (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  patient_id UUID REFERENCES patients(id),
  diary_date DATE NOT NULL,              -- 기록 날짜
  bedtime TIME,                          -- 취침 시간
  wake_time TIME,                        -- 기상 시간
  sleep_onset_latency VARCHAR(20),       -- 잠들기까지 걸린 시간 (선택값)
  night_awakening_count VARCHAR(20),     -- 야간 각성 횟수 (선택값)
  sleep_event_memo TEXT,                 -- 수면 중 특이사항 메모 (선택, 최대 200자)
  sleep_quality SMALLINT,               -- 수면 만족도 1~5
  morning_fatigue SMALLINT,             -- 기상 시 피로도 1~5
  daytime_sleepiness VARCHAR(20),       -- 낮 동안 졸림 (선택값)
  nap_taken BOOLEAN DEFAULT FALSE,      -- 낮잠 여부
  nap_duration_min INT,                 -- 낮잠 시간 (분)
  dream VARCHAR(20),                    -- 꿈 여부 (선택값)
  caffeine VARCHAR(20),                 -- 카페인 섭취 (선택값)
  alcohol BOOLEAN DEFAULT FALSE,        -- 음주 여부
  condition SMALLINT,                   -- 오늘 컨디션 1~5
  memo TEXT,                            -- 특이사항 메모
  herbal_morning BOOLEAN,               -- 한약 아침 복약 여부
  herbal_lunch BOOLEAN,                 -- 한약 점심 복약 여부
  herbal_evening BOOLEAN,               -- 한약 저녁 복약 여부
  herbal_bedtime BOOLEAN,               -- 한약 취침전 복약 여부
  western_morning BOOLEAN,              -- 양약 아침 복약 여부
  western_lunch BOOLEAN,                -- 양약 점심 복약 여부
  western_evening BOOLEAN,              -- 양약 저녁 복약 여부
  western_bedtime BOOLEAN,              -- 양약 취침전 복약 여부
  -- 관리자 입력: 객관적 수면 데이터 (웨어러블/PSG 측정값)
  total_sleep_min INT,                  -- 총 수면 시간 (분)
  deep_sleep_min INT,                   -- 깊은 수면 시간 (분, Non-REM 3단계)
  light_sleep_min INT,                  -- 얕은 수면 시간 (분, Non-REM 1~2단계)
  rem_sleep_min INT,                    -- REM 수면 시간 (분)
  admin_note TEXT,                      -- 관리자 메모
  created_at TIMESTAMP DEFAULT NOW(),
  updated_by UUID REFERENCES auth.users(id), -- 수정한 관리자 ID
  updated_at TIMESTAMP                       -- 수정 일시
);
```

### 4.5 exam_results 테이블 (신규)

```sql
CREATE TABLE exam_results (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  patient_id UUID REFERENCES patients(id),
  exam_date DATE NOT NULL,              -- 검사 날짜
  exam_type VARCHAR(20) NOT NULL,       -- 'HRV' | 'InBody' | 'QEEG'
  result_data JSONB,                    -- 검사 항목별 수치 (유연한 구조 저장)
  summary TEXT,                         -- 원장 코멘트 / 해석 메모
  created_by UUID REFERENCES auth.users(id),
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);
```

### 4.6 isi_assessments 테이블 (신규)

```sql
CREATE TABLE isi_assessments (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  patient_id UUID REFERENCES patients(id),
  assessed_at DATE NOT NULL,            -- 자가진단 날짜
  q1 SMALLINT,                          -- 잠들기 어려움 (0~4)
  q2 SMALLINT,                          -- 수면 유지 어려움 (0~4)
  q3 SMALLINT,                          -- 너무 일찍 깸 (0~4)
  q4 SMALLINT,                          -- 수면 패턴 만족도 (0~4)
  q5 SMALLINT,                          -- 낮 기능 저하 (0~4)
  q6 SMALLINT,                          -- 수면 문제 인지 정도 (0~4)
  q7 SMALLINT,                          -- 수면 문제 걱정 정도 (0~4)
  total_score SMALLINT GENERATED ALWAYS AS (q1+q2+q3+q4+q5+q6+q7) STORED, -- 합산 점수 (0~28)
  created_at TIMESTAMP DEFAULT NOW()
);
-- 점수 해석: 0~7 없음 / 8~14 경미 / 15~21 중등도 / 22~28 심각
```

### 4.7 settings 테이블 (신규)

```sql
CREATE TABLE settings (
  id UUID PRIMARY KEY REFERENCES auth.users(id),
  push_enabled BOOLEAN DEFAULT FALSE,   -- 푸시 알림 수신 여부 (기본값: OFF)
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);
```

### 4.8 qna 테이블

```sql
CREATE TABLE qna (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  patient_id UUID REFERENCES patients(id),
  question TEXT NOT NULL,
  answer TEXT,
  is_answered BOOLEAN DEFAULT FALSE,
  answered_by UUID REFERENCES auth.users(id),
  answered_at TIMESTAMP,
  created_at TIMESTAMP DEFAULT NOW()
);
```

### 4.9 users 테이블 (Supabase Auth 연동)

```sql
CREATE TABLE user_roles (
  id UUID PRIMARY KEY REFERENCES auth.users(id),
  role VARCHAR(20) NOT NULL, -- 'patient' or 'admin'
  patient_id UUID REFERENCES patients(id),
  created_at TIMESTAMP DEFAULT NOW()
);
```

---

## 5. 주요 화면 구성

### 5.1 환자용 화면 (모바일 우선 설계)

<aside>
📱

환자는 주로 스마트폰으로 접근하므로, 모바일 친화적 UI를 최우선으로 설계한다.

하단 탭 내비게이션, 큰 버튼, 간결한 입력 폼을 기본 원칙으로 한다.

</aside>

**하단 탭 구조 (4개)**

- 🏠 **홈** — 오늘의 수면 일지 작성, 복약 체크, 다음 방문일 안내
- 📊 **기록** — 수면 패턴 캘린더/그래프, 수면 단계별 스택 바 차트 (총 수면·깊은 수면·얕은 수면·REM), **수면 효율(%) 꺾은선 그래프**, 복약 이력, 증상 변화 타임라인, **검사 결과(HRV·InBody·QEEG) 날짜별 열람**, **ISI 자가진단 점수 그래프**
- 💊 **처방** — 현재 처방된 한약명, 복약 지시사항, 처방 기간 조회
- 📋 **자가진단** — ISI 7문항 직접 체크 및 제출, 점수 즉시 확인, 날짜별 꺾은선 그래프
- 💬 **문의** — Q&A 문의 작성, 답변 확인, 알림 수신

**UI 설계 원칙**

- 수면 일지·복약 체크는 탭 1회로 완료할 수 있는 간단한 형태
- 텍스트 입력 최소화, 슬라이더·버튼 선택 방식 활용
- 홈 화면은 개인화: 처방이 있는 환자에게만 '복약 체크' 버튼 노출, 처방 없는 환자는 미노출
- 홈 화면에서 오늘 해야 할 항목(일지 미작성, 복약 미체크)을 즉시 확인 가능
- 알림은 **기본 OFF**이며, 환자가 설정 메뉴에서 토글 스위치로 직접 ON할 수 있음 (수면 일지 작성 리마인드, 복약 알림, Q&A 답변 알림 등 항목별 독립 설정)
- PWA(Progressive Web App) 방식으로 앱 설치 없이 홈 화면 추가 지원

### 5.2 관리자용 화면 (데스크탑 우선 설계)

<aside>
🖥️

관리자는 주로 PC 브라우저로 접근하므로, 다수의 환자 데이터를 한눈에 파악할 수 있는

넓은 레이아웃과 테이블 중심 UI를 기본 원칙으로 한다.

</aside>

**사이드바 메뉴 구조**

- 📊 **대시보드** — 재택 관리 현황 요약, 미활동 환자 알림, 임박한 재방문 일정, 미답변 Q&A 수
- 👥 **환자 목록** — 전체 환자 테이블 (수면 일지 최근 입력일·복약 순응도·다음 방문일 포함), 검색·필터·정렬. '처방 환자'만 따로 필터링하여 복약 순응도 집중 관리 가능
- 🔍 **환자 상세** — 개별 환자의 수면 일지 그래프, 복약 체크 이력, 증상 변화 타임라인 전체 조회. 날짜별 수면 일지 항목 수정 가능 (수정 이력 자동 저장)
- ✏️ **환자 등록/수정** — 신규 환자 등록, 진단명·중증도·처방 정보 입력 및 수정, 재방문 일정 설정
- 📋 **수면 데이터 입력** — 날짜별 총 수면 시간·깊은 수면·얕은 수면·REM 수면 시간 직접 입력 (웨어러블·PSG 측정값 기반)
- 🔬 **검사 결과 입력** — HRV·InBody·QEEG 검사 날짜 및 항목별 수치 입력, 원장 코멘트 등록. 입력 즉시 환자 기록 탭에 노출
- 💬 **Q&A 관리** — 전체 문의 목록, 미답변 우선 필터, 답변 작성 및 등록

**UI 설계 원칙**

- 대시보드는 한 화면에서 핵심 지표를 모두 파악할 수 있는 멀티 패널 레이아웃
- 환자 목록은 정렬·필터가 즉시 반응하는 인터랙티브 테이블
- 환자 상세 페이지는 좌측 요약 정보 + 우측 상세 기록 2단 레이아웃
- 수면 효율 지표는 대시보드 현황 카드에 포함하여 한눈에 파악 가능

---

## 6. 보안 요구사항

### 6.1 데이터 보호

- **개인정보 암호화:** 이름, 생년월일, 연락처 등 민감 정보는 저장 시 암호화 (AES-256 권장)
- **비밀번호 해싱:** 환자 비밀번호는 bcrypt 등 단방향 해시 알고리즘으로 저장. 평문 저장 절대 금지
- **HTTPS:** 모든 클라이언트↔서버 통신에 SSL/TLS 적용. HTTP 접속 시 자동 리다이렉트
- **민감 데이터 최소 수집:** 진료에 필요한 최소한의 개인정보만 수집하고, 불필요한 항목은 수집하지 않음

### 6.2 접근 제어

- **Row Level Security (RLS):** Supabase RLS 정책으로 환자는 본인 데이터만, 관리자는 전체 데이터에 접근 가능하도록 DB 레벨에서 강제
- **JWT 토큰 관리:** 로그인 토큰은 만료 시간 설정 (예: 액세스 토큰 1시간, 리프레시 토큰 7일). 로그아웃 시 서버 측 토큰 무효화
- **관리자 2단계 인증(2FA):** 관리자 로그인 시 OTP 또는 이메일 인증 코드 추가 검증 권장
- **세션 자동 만료:** 일정 시간 비활동 시 자동 로그아웃 처리 (환자 30분, 관리자 15분 권장)

### 6.3 API 보안

- **API 인증 필수:** 모든 API 엔드포인트는 유효한 JWT 토큰 없이 접근 불가
- **입력값 검증:** 서버 측에서 모든 입력값 유효성 검사. SQL Injection, XSS 방지
- **Rate Limiting:** 로그인 시도 횟수 제한 (예: 5회 실패 시 10분 잠금) 으로 무차별 대입 공격 방지
- **CORS 설정:** 허용된 도메인 외 외부 출처 요청 차단

### 6.4 감사 및 모니터링

- **감사 로그:** 관리자의 모든 데이터 조회·수정·삭제 작업을 로그로 기록 (누가, 언제, 무엇을, 어떤 값으로)
- **비정상 접근 감지:** 짧은 시간 내 다수 요청, 비인가 접근 시도 등 이상 징후 모니터링
- **로그 보존 기간:** 감사 로그는 최소 1년 이상 보존

### 6.5 인프라 및 운영

- **정기 백업:** Supabase 자동 백업 설정 (일 1회 이상). 백업 데이터는 별도 보관
- **환경변수 관리:** API 키, DB 접속 정보 등 민감 값은 환경변수로 관리. 코드에 하드코딩 금지
- **의존성 관리:** 사용 중인 패키지의 보안 취약점 정기 점검 (npm audit 등)
- **개인정보처리방침:** 서비스 내 개인정보 수집·이용 목적 및 보관 기간 고지 페이지 제공

---

## 7. 개발 단계

### Phase 1: 기본 인프라 구축 (1-2주)

- Vercel + Next.js 프로젝트 생성
- Supabase 프로젝트 생성 및 데이터베이스 스키마 설정
- 인증 시스템 구현 (환자/관리자 분리)

### Phase 2: 환자 관리 기능 (2-3주)

- 환자 등록/수정/삭제 기능
- 수면장애 정보 입력 및 관리
- 치료 기록 관리

### Phase 3: 관리자 대시보드 (2-3주)

- 통계 데이터 집계 로직
- 차트 및 시각화 구현
- 환자 검색 및 필터링

### Phase 4: 테스트 및 배포 (1-2주)

- 기능 테스트 및 버그 수정
- 보안 점검
- 프로덕션 배포

---

## 8. 성공 지표

- **시스템 안정성:** 99% 이상 가동률
- **사용자 만족도:** 관리자의 업무 효율성 향상
- **데이터 정확성:** 환자 정보 입력 오류율 최소화
- **응답 속도:** 페이지 로딩 시간 2초 이내

---

## 9. 향후 확장 계획

- **모바일 앱:** React Native 기반 네이티브 앱 개발
- **AI 연동:** 수면장애 처방 AI와 연동하여 추천 기능 제공
- **알림 기능:** 이메일/SMS 자동 발송 (재방문 알림, 처방 알림 등)
- **데이터 분석:** 머신러닝 기반 패턴 분석 및 예측
- **환자 포털:** 환자가 직접 증상 일지 작성, 수면 패턴 기록 등